name: Build

on:
  push:
    tags: [v*.*.*]
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: write
jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v3
      - run: rustup toolchain install stable --profile minimal
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "app"
      - name: Build
        if: ${{ startsWith(github.ref, 'refs/tags/') == false }}
        run: cargo build
      - name: Build(release)
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: cargo build --release
      - uses: actions/upload-artifact@v3
        with:
          name: artifact-${{ matrix.os }}
          path: |
            ./target/*/packet-flow*
            !./target/*/packet-flow.d
      - name: Package
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
              7z a packet-flow_"$RUNNER_OS"_x64.zip ./target/release/packet-flow.exe
          else
              tar czvf packet-flow_"$RUNNER_OS"_x64.tgz ./target/release/packet-flow
          fi
      - name: Release
        uses: softprops/action-gh-release@v1
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        with:
          files: |
            ./packet-flow_*_x64.*
