name: Build

on:
  push:
    tags: [v*.*.*]
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: write
jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v3
      - run: rustup toolchain install stable --profile minimal
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "app"
      - name: Build
        if: ${{ startsWith(github.ref, 'refs/tags/') == false }}
        run: cargo build
      - name: Build(release)
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: cargo build --release
      - uses: actions/upload-artifact@v3
        with:
          name: artifact-${{ matrix.os }}
          path: |
            ./target/*/packet-flow
      - name: Package
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: |
          tar czvf packet-flow_"$RUNNER_OS"_x64.tgz ./target/release/packet-flow
      - name: Release
        uses: softprops/action-gh-release@v1
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        with:
          files: |
            ./packet-flow_*_x64.tgz
